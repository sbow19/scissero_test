// Define jsdocs types across application

/**
 * @typedef {Array<Array<number>>} TableData
 * 
 * @typedef {Object} Arguments
 * @property {number} noteValue - default amount of 1000
 * @property {number} bufferThreshold  - final value of Underlying when Contingent Interest is payable
 * @property {number} buffer    - amount of negative Underlying decrease tolerated
 * @property {number} years     - number of years until maturity date
 * @property {number} interestRate - interest per annum represented as percentage points
 * @property {number} tableGradations - number of rows in final table
 */

export {}